version: "1"

env:
  schema:
    REDIS_PORT:
      type: int
      required: true

services:
  redis:
    health:
      type: tcp
      host: localhost
      port: 16379
      timeout: 30s
      interval: 1s
